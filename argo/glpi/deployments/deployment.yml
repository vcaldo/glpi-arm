apiVersion: apps/v1
kind: Deployment
metadata:
  name: glpi-deploy
  labels:
    app: glpi-stack
  annotations:
spec:
  selector:
    matchLabels:
      app: glpi-stack
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: glpi-stack
    spec:
      containers:
      - name: glpi
        image: vcaldo/glpi:glpi-9.5.1
        imagePullPolicy: Always
        envFrom:
          - configMapRef:
              name: glpi-configmap.yml
        ports:
        - containerPort: 5000
        resources:
          requests:
            memory: "256Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        volumeMounts:
        - name: glpi
          mountPath: /var/www/html
      - name: cron
        image: vcaldo/glpi:glpi-cron-9.5.1
        imagePullPolicy: Always
        envFrom:
          - configMapRef:
              name: glpi-configmap.yml
        resources:
          requests:
            memory: "256Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        volumeMounts:
        - name: glpi
          mountPath: /var/www/html
      - name: mariadb
        image: vcaldo/glpi:glpi-mariadb-10.3
        imagePullPolicy: Always
        envFrom:
          - configMapRef:
              name: mariadb-configmap.yml
        resources:
          requests:
            memory: "256Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        volumeMounts:
        - name: mariadb
          mountPath: /var/lib/mysql
      volumes:
        - name: mariadb
          persistentVolumeClaim:
            claimName: glpi-mariadb
        - name: glpi
          persistentVolumeClaim:
            claimName: glpi-glpi